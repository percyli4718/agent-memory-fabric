name: CI Placeholder

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Placeholder
        run: echo "Add lint, schema validation, tests, SBOM, and provenance checks here."
